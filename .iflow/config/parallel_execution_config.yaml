# 🌟 A项目并行执行引擎配置文件 V7
# 全面优化的系统配置，支持高性能并行执行
# 你一定要超级思考、极限思考、深度思考，全力思考、超强思考，认真仔细思考（ultrathink、think really super hard、think intensely）

# 核心系统配置
system:
  # 系统标识
  version: "V7.0"
  name: "A项目并行执行引擎"
  description: "高性能智能并行执行系统"
  
  # 执行模式
  execution_mode: "parallel_optimized"  # parallel_simple, parallel_optimized, quantum_enhanced
  parallel_strategy: "adaptive"  # static, dynamic, adaptive
  
  # 系统资源
  max_memory_usage: "8GB"
  max_cpu_usage: 80  # 百分比
  max_concurrent_tasks: 50
  max_parallel_agents: 20
  
  # 超时配置
  task_timeout: 300  # 秒
  agent_timeout: 60   # 秒
  workflow_timeout: 1800  # 秒 (30分钟)

# 缓存系统配置
cache:
  # 缓存基础配置
  enabled: true
  max_size: 1000  # 最大缓存条目数
  ttl_hours: 24   # 缓存生存时间
  cleanup_interval: 3600  # 清理间隔(秒)
  
  # 缓存策略
  eviction_policy: "lru"  # lru, fifo, random
  compression_enabled: true
  serialization_format: "pickle"  # pickle, json, msgpack
  
  # 预计算配置
  precomputation_enabled: true
  pattern_learning_rate: 0.1
  similarity_threshold: 0.8
  
  # 内存管理
  memory_threshold_mb: 500
  auto_cleanup_enabled: true
  persistent_storage: true

# 智能体并行执行配置
parallel_agents:
  # 基础配置
  max_concurrent_agents: 16
  agent_startup_timeout: 30
  agent_heartbeat_interval: 10
  
  # 资源分配
  cpu_per_agent: 25  # 百分比
  memory_per_agent: "512MB"
  network_bandwidth_limit: "100MB/s"
  
  # 调度策略
  scheduling_algorithm: "priority_aware"  # round_robin, priority_aware, load_balanced
  load_balancing_enabled: true
  failover_enabled: true
  
  # 智能体类型配置
  agent_types:
    specialist:
      max_instances: 8
      priority: 10
      resource_multiplier: 1.0
    coordinator:
      max_instances: 2
      priority: 15
      resource_multiplier: 1.5
    validator:
      max_instances: 4
      priority: 8
      resource_multiplier: 0.8
    integrator:
      max_instances: 3
      priority: 12
      resource_multiplier: 1.2

# 任务分解器配置
task_decomposer:
  # 分解策略
  max_subtasks_per_task: 20
  min_subtask_complexity: 0.5
  max_subtask_complexity: 8.0
  
  # 并行化配置
  auto_parallelization: true
  dependency_analysis_depth: 3
  parallelization_threshold: 0.6
  
  # 复杂度分析
  complexity_analysis_enabled: true
  domain_weighting_enabled: true
  historical_data_weight: 0.3
  
  # 优化配置
  task_refinement_enabled: true
  redundancy_detection: true
  conflict_resolution_strategy: "priority_based"

# 工作流阶段并行配置
workflow_parallelizer:
  # 阶段管理
  max_concurrent_stages: 8
  stage_timeout: 600  # 秒
  stage_retry_limit: 3
  
  # 依赖管理
  dependency_resolution_timeout: 30
  circular_dependency_detection: true
  automatic_dependency_inference: true
  
  # 资源管理
  resource_allocation_strategy: "fair_sharing"  # fair_sharing, priority_based, greedy
  resource_conflict_resolution: "queue_based"
  
  # 性能优化
  pipeline_execution: true
  speculative_execution: false  # 可能增加资源使用
  checkpointing_enabled: true

# 性能优化配置
performance:
  # 并行优化
  parallel_execution_threshold: 2  # 最小并行任务数
  load_threshold: 0.7  # 负载阈值
  auto_scaling_enabled: true
  scaling_cooldown: 60  # 秒
  
  # 缓存优化
  cache_warmup_enabled: true
  predictive_caching: true
  cache_invalidation_strategy: "ttl_based"
  
  # 内存优化
  garbage_collection_interval: 300
  memory_pool_size: "1GB"
  object_pooling_enabled: true
  
  # 网络优化
  connection_pool_size: 50
  request_timeout: 30
  retry_attempts: 3

# 监控和日志配置
monitoring:
  # 性能监控
  performance_monitoring_enabled: true
  metrics_collection_interval: 60  # 秒
  metrics_retention_days: 30
  
  # 健康检查
  health_check_interval: 30
  health_check_timeout: 10
  auto_recovery_enabled: true
  
  # 日志配置
  log_level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  log_format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  log_rotation: "daily"
  log_max_files: 30
  
  # 追踪配置
  tracing_enabled: true
  trace_sample_rate: 0.1  # 10%的请求被追踪
  distributed_tracing: true

# 安全配置
security:
  # 访问控制
  authentication_required: false  # 开发环境可关闭
  authorization_enabled: true
  role_based_access: true
  
  # 数据安全
  encryption_enabled: false  # 生产环境应启用
  sensitive_data_masking: true
  
  # 执行安全
  sandbox_execution: false  # 开发环境可关闭
  resource_limits_enforced: true
  malicious_code_detection: true

# 模型适配配置
model_adapter:
  # 模型选择策略
  auto_model_selection: true
  model_preference: ["openai", "anthropic", "google", "local"]
  fallback_strategy: "next_available"
  
  # 调用优化
  max_concurrent_calls: 10
  call_timeout: 60
  retry_on_failure: true
  exponential_backoff: true
  
  # 成本控制
  cost_optimization_enabled: true
  budget_limit_per_hour: 10.0  # 美元
  cost_alert_threshold: 0.8

# ARQ和意识流配置
arq_consciousness:
  # ARQ引擎配置
  arq_enabled: true
  reasoning_depth: 5
  compliance_check_frequency: 10
  
  # 意识流配置
  consciousness_stream_enabled: true
  memory_retention_hours: 168  # 7天
  long_term_memory_enabled: true
  
  # 学习配置
  continuous_learning: true
  pattern_recognition_enabled: true
  adaptive_rule_evolution: true

# 部署配置
deployment:
  # 环境配置
  environment: "development"  # development, staging, production
  
  # 容器化配置
  docker_enabled: false
  kubernetes_enabled: false
  scaling_strategy: "horizontal"
  
  # 高可用性
  high_availability: false
  replication_factor: 1
  disaster_recovery_enabled: false
  
  # 更新策略
  auto_update_enabled: false
  update_check_interval: 86400  # 24小时
  rollback_on_failure: true

# 高级配置
advanced:
  # 量子优化 (实验性)
  quantum_optimization_enabled: false
  quantum_annealing_enabled: false
  quantum_simulation_enabled: false
  
  # 机器学习优化
  ml_optimization_enabled: true
  reinforcement_learning_enabled: true
  neural_network_optimization: true
  
  # 分布式计算
  distributed_computing_enabled: false
  cluster_mode: "standalone"
  node_discovery_enabled: false
  
  # 实验性功能
  experimental_features_enabled: false
  feature_flags:
    "adaptive_quantum_annealing": false
    "neural_architecture_search": false
    "meta_learning_optimization": false

# 特定场景配置
scenarios:
  # 高性能计算场景
  high_performance:
    max_parallel_agents: 32
    cache_size: 2000
    memory_optimization: true
    cpu_affinity_enabled: true
  
  # 内存受限场景
  memory_constrained:
    max_memory_usage: "2GB"
    cache_size: 500
    compression_enabled: true
    swap_enabled: true
  
  # 低延迟场景
  low_latency:
    optimization_target: "latency"
    parallel_threshold: 1
    cache_warmup_enabled: true
    precomputation_enabled: true
  
  # 批处理场景
  batch_processing:
    optimization_target: "throughput"
    max_batch_size: 100
    pipeline_execution: true
    resource_sharing: true

# 自定义配置模板
custom_profiles:
  # 开发环境配置
  development:
    log_level: "DEBUG"
    cache_enabled: false
    performance_monitoring: true
    auto_scaling: false
  
  # 测试环境配置
  testing:
    log_level: "INFO"
    cache_enabled: true
    performance_monitoring: true
    auto_scaling: true
    sandbox_execution: true
  
  # 生产环境配置
  production:
    log_level: "WARNING"
    cache_enabled: true
    performance_monitoring: true
    auto_scaling: true
    high_availability: true
    encryption_enabled: true
    sandbox_execution: true

# 配置验证
validation:
  enabled: true
  strict_mode: false
  warnings_as_errors: false
  configuration_checksum: true